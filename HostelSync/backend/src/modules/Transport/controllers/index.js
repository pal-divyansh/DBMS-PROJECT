const {
  getAvailableVehicles,
  getRoutes,
  createBooking,
  getUserBookings,
  cancelBooking
} = require('./transport.controller');

module.exports = {
  getAvailableVehicles,
  getRoutes,
  createBooking,
  getUserBookings,
  cancelBooking
};
